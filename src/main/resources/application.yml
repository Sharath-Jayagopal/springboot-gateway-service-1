spring:
  application:
    name: gateway
  cloud:
    gateway:
      mvc:
        routes:
          - id: microservice
            uri: http://localhost:8081
            predicates:
              - Path=/microservice/**

server:
  port: 8080
